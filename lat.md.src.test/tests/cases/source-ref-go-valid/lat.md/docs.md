# Docs

See [[src/app.go#Greet]] for the greeting function.

The [[src/app.go#Greeter]] struct has a method [[src/app.go#Greeter#Greet]].

Create one with [[src/app.go#NewGreeter]].

The [[src/app.go#Greeting]] interface defines the contract.

Default name is [[src/app.go#DefaultName]].
