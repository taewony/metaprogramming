# Notes

This paragraph is intentionally written to be extremely long so that it exceeds the maximum allowed character limit for a leading section paragraph. The limit exists to ensure that every section begins with a concise overview that fits well in search results and command output. This sentence pushes the total well past the two hundred and fifty character threshold we are validating.

## With Links

This section has a [[long/path/to/some/deeply/nested/section#Very#Deep#Heading]] that makes raw text long but the counted length excludes link content entirely.
